/**
 * Default initial password assigned to every artisan when their application
 * is approved. The artisan can change it using "¿Olvidaste tu contraseña?"
 * or sign in with Google instead.
 */
export const DEFAULT_ARTISAN_PASSWORD = "orfebre123";
